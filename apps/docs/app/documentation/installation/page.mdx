---
asIndexPage: true
---

import { Cards } from "nextra/components";
import { MarkdownIcon } from "nextra/icons";
import { MDXRemote } from "nextra/mdx-remote";
import { createIndexPage, getPageMap } from "nextra/page-map";

# Guide

The following features are configured via the Next.js configuration and are
available in all themes.

<MDXRemote
  compiledSource={await createIndexPage(
    await getPageMap("/documentation/installation")
  )}
  components={{
    Cards,
  }}
/>
