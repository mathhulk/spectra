{
  "name": "spectra",
  "scripts": {
    "build": "turbo run build"
  },
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
